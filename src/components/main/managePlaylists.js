import React from "react";
import LoadingSpinner from "../loadingSpinner";
const ManagePlaylists = props => (
    <section className="manage-playlists">
        <LoadingSpinner />
    </section>
);

export default ManagePlaylists;
