const defaultState = {
    isFetching: false,
    id: null,
    items: []
};

const selectedPlaylist = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default selectedPlaylist;
